{
  "name": "crimenet-blockchain",
  "version": "1.0.0",
  "description": "CrimeNet AI — Smart contracts for evidence integrity, immutable audit trail, criminal records, report certificates and inter-agency sharing",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "verify": "hardhat run scripts/verify.js --network goerli",
    "node": "hardhat node"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0",
    "ethers": "^6.7.0"
  }
}
